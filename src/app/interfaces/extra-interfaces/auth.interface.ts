// import { AuthTM, AuthVM, AuthCM } from 'src/app/view-models';

// export interface IAuthController {
    // readonly authorized: () => Promise<AuthVM>;
    // readonly login: (body: AuthCM) => Promise<AuthTM>;
// }
